# ---------- 版本管理 ----------
.git
.gitignore

# ---------- 本地运行产物 / 数据（不进镜像） ----------
data/
*.db
*.db-shm
*.db-wal
smoke_*.txt

# ---------- 参考文档（增大构建上下文，且非运行所需） ----------
docs/

# ---------- 环境与密钥（绝不进镜像，运行时由 compose env_file 注入） ----------
.env
.env.example

# ---------- 构建 / 日志 ----------
*.log
Dockerfile
docker-compose.yml
